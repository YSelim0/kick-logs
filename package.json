{
  "name": "kick-logs",
  "private": true,
  "packageManager": "pnpm@8.11.0",
  "scripts": {
    "dev": "pnpm --filter @kick-logs/web dev",
    "build": "pnpm --filter @kick-logs/web build",
    "lint": "pnpm --filter @kick-logs/web lint",
    "typecheck": "pnpm --filter @kick-logs/web typecheck",
    "test": "pnpm --filter @kick-logs/web test",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.11.0"
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
