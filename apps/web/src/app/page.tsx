import { RouteShell } from "@/components/layout/route-shell";

export default function HomePage() {
  return (
    <RouteShell eyebrow="Reserved" title="Root Route">
      Landing content is intentionally deferred until the application screens are complete.
    </RouteShell>
  );
}
